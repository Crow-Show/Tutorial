[Типы данных](https://metanit.com/sharp/tutorial/2.1.php)

Создать программу, которая:

🔹 Шаг 1: Запрашивает три значения

first (double)

second (double)

third (double)

Используем double.Parse(Console.ReadLine()).

🔹 Шаг 2: Объявляет константы
const double AddFactor = 2;
const double MultiplyFactor = 1.5;
const double Divider = 3;

🔹 Шаг 3: Выполняет вычисления
result1 = (first + second) * AddFactor;
result2 = second * MultiplyFactor;
result3 = (first + second + third) / Divider;
result4 = (second - first) * (third + AddFactor);

🔹 Шаг 4: Выводит аккуратный блок с результатами
```
===========================
Входные значения:
A = ...
B = ...
C = ...

Результаты вычислений:
1: ...
2: ...
3: ...
4: ...
===========================
```

🔹 Требования

Переменные — camelCase

Константы — PascalCase

Использовать интерполяцию

Скобки для подчёркивания порядка вычислений