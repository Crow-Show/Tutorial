[Методы](https://metanit.com/sharp/tutorial/2.8.php)    
[Параметры методов](https://metanit.com/sharp/tutorial/2.9.php)     
[Возвращение и return](https://metanit.com/sharp/tutorial/2.34.php)     
# ЗАДАНИЕ: Консольная программа «Менеджер расчетов заказов»
*(Тема: Методы, параметры, возвращение значения)*

---

## 📌 Описание

Требуется написать **консольную программу**, которая помогает менеджеру быстро рассчитать стоимость и бонусы для нескольких заказов.

Программа должна быть **разделена на отдельные методы**, каждый из которых отвечает за **одну конкретную задачу**, чтобы код был логичным и структурированным.

Важно: методы должны использовать **параметры и возвращаемое значение**, избегая дублирования кода.

---

## 🔧 Требования к программе

### 1. Ввод данных о заказах
- Пользователь вводит количество заказов.
- Для каждого заказа вводятся:
    - имя клиента (строка)
    - сумма заказа (double, положительное число)
    - категория клиента: `"Стандарт"`, `"Премиум"`, `"VIP"`

Все проверки корректности ввода должны быть выполнены в **отдельных методах**.

---

### 2. Расчет скидки
- Для каждой категории клиента программа должна вычислять скидку:
    - Стандарт → 0%
    - Премиум → 5%
    - VIP → 10%

Этот расчет выполняется в **отдельном методе**, который принимает категорию и сумму заказа, возвращает сумму со скидкой.

---

### 3. Расчет бонуса
- Для каждого заказа вычисляется бонус в зависимости от суммы после скидки:
    - до 1000 → 1%
    - 1000–5000 → 2%
    - больше 5000 → 3%

Бонус вычисляется в **отдельном методе** и возвращается как значение.

---

### 4. Итоговая обработка
- Для каждого заказа нужно вывести:
    - имя клиента
    - сумма заказа до скидки
    - сумма после скидки
    - бонус

Вся логика суммирования должна быть вынесена в отдельный метод.

---

## 🔹 Требования по архитектуре

1. **Каждая логическая часть** — свой метод:
    - ввод данных
    - проверка корректности
    - расчет скидки
    - расчет бонуса
    - суммирование и вывод
2. Методы должны **принимать параметры** и **возвращать значения**, где это логично.
3. **Не дублировать код** — если часть логики повторяется, оформить отдельным методом.
4. **Читаемость и структура** важны: названия методов и переменных должны отражать суть.
5. **Комментарии** — кратко пояснять каждый метод и блок кода.

---

## 📌 Ограничения

- Использовать только знания глав **до методов включительно** (условия, циклы, арифметика, консольный ввод/вывод, переменные, константы).
- **Массивы или списки пока не использовать** — все заказы можно хранить в отдельных переменных или обрабатывать по циклу для каждого заказа.
