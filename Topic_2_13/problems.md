# ЗАДАНИЕ: Анализ загрузки сервера по дням (зубчатые массивы)

*(Глава: «Массивы» — с использованием циклов и базовой логики)*

---

## 📌 Описание задачи

Ты разрабатываешь консольную программу для анализа логов загрузки сервера.

Сервер в течение нескольких дней сохранял значения загрузки CPU (в процентах).
В каждый день выполнялось **разное количество измерений** — в зависимости от активности.

Твоя задача — считать эти данные, сохранить их в **зубчатом массиве** и выполнить анализ.

---

## 📥 Ввод данных

1. Пользователь вводит **количество дней наблюдений**.
2. Для каждого дня:
    - вводит количество измерений за этот день;
    - затем вводит сами значения загрузки CPU (целые числа от 0 до 100).

Все данные хранятся в зубчатом массиве: `int[][] cpuLoadByDay;`


- каждая строка — один день;
- каждый элемент строки — одно измерение загрузки CPU.

---

## 🧠 Требования к логике программы

### 🔹 Для каждого дня необходимо определить:
- количество измерений;
- минимальную загрузку;
- максимальную загрузку;
- среднюю загрузку за день.

---

### 🔹 Общий анализ по всем дням:
- общее количество измерений;
- минимальное значение за все дни;
- максимальное значение за все дни;
- день с **наибольшим средним значением загрузки**;
- день с **наибольшим количеством измерений**.

> Нумерация дней — с 1.

---

## 📊 Вывод результатов

### 1️⃣ Таблица по дням

Вывести таблицу следующего вида:
```
День | Кол-во | Мин | Макс | Среднее

1 | 5 | 20 | 80 | 50.0
2 | 3 | 10 | 40 | 25.0
3 | 6 | 30 | 90 | 60.0
```

Форматирование сделать с помощью:
- интерполяции строк;
- выравнивания (`PadLeft`, `PadRight`) — по необходимости.

---

### 2️⃣ Итоговый отчёт

После таблицы вывести:

- день с максимальной средней загрузкой;
- день с максимальным количеством измерений;
- общий минимум и максимум по всем данным.

---

## ⭐ Дополнительное задание (обязательное)

1. Создать **одномерный массив**, содержащий **все измерения из всех дней**.
2. Отсортировать этот массив по возрастанию.
3. Вывести отсортированные значения в одну строку.

> ❗ Использовать:
> - циклы (`for`, `foreach`, `while`);
> - `Array.Sort`;
> - без методов и без LINQ.

---

## ⚠️ Ограничения

- ❌ Нельзя использовать методы.
- ❌ Нельзя использовать LINQ.
- ❌ Нельзя использовать классы и ООП.
- ✅ Можно использовать любые циклы.
- ✅ Можно использовать только конструкции и темы до главы «Массивы» включительно.

---

## 🎯 Цель задания

- Понять, **зачем нужен зубчатый массив**.
- Научиться работать с массивами разной длины.
- Научиться агрегировать данные.
- Понять, как из сложной структуры получить плоский массив.
- Потренироваться в архитектуре и читаемости кода.
