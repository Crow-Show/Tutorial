# ЗАДАНИЕ: Консольный симулятор движения персонажа
*(Тема: конструкция switch + перечисления enum)*

## 📌 Описание

Требуется написать консольную программу, которая симулирует движение персонажа по игровому полю.

Персонаж находится на двумерной сетке фиксированного размера (например, 10×10).  
У персонажа есть координаты X и Y.

Направления движения описываются через перечисление (enum).

Пользователь вводит команды управления, программа:
- интерпретирует команду,
- определяет направление движения,
- изменяет координаты персонажа,
- не позволяет выйти за границы поля,
- выводит результат каждого шага в виде таблицы.

---

## 🎯 Цель задания

- Осмысленно использовать enum и switch
- Показать, что enum — это модель предметной области, а не просто набор чисел
- Научиться писать switch с реальной логикой, а не только выводом текста
- Структурировать код: ввод → обработка → изменение состояния → вывод
- Подготовка к стилю мышления, близкому к коммерческой разработке

---

## 📦 Перечисление направлений

В программе должно быть объявлено перечисление направлений движения персонажа:

Direction:
- Up
- Down
- Left
- Right

---

## 📥 Ввод пользователя

Пользователь вводит команды управления:

- "w" — движение вверх
- "s" — движение вниз
- "a" — движение влево
- "d" — движение вправо
- "stop" — завершение программы

Если команда введена некорректно:
- персонаж не двигается
- программа выводит комментарий о неверной команде

---

## 🔧 Логика программы

1. Задать размеры игрового поля (например, ширина и высота).
2. Задать стартовые координаты персонажа (например, центр поля).
3. Завести счётчик шагов.
4. В цикле:
    - считать команду пользователя
    - если команда "stop" — завершить программу
    - через switch:
        - преобразовать команду в значение enum Direction
        - определить, возможно ли движение (проверка границ поля)
        - изменить координаты или отказать в движении
        - сформировать текст комментария
5. После каждого шага выводить строку таблицы.

---

## ⚠️ Обязательные требования

- Enum Direction обязателен
- Switch обязателен и должен содержать реальную логику
- Нельзя напрямую менять координаты без проверки границ
- Нельзя обрабатывать всё в одном месте без разделения логики
- Код должен быть читаемым, с осмысленными именами переменных
- Комментарии — только там, где без них логика неочевидна

---

## ❌ Типичные ошибки (будут считаться проблемами)

- Switch, в котором только Console.WriteLine
- Магические числа без объяснения
- Логика движения и вывода в одной куче
- Отсутствие обработки некорректного ввода
- Переименование enum в int «для удобства»

---

## 🧠 Ожидаемый уровень решения

Это задание уровня:
- уверенный beginner / начинающий junior
- код должен выглядеть так, чтобы его не было стыдно показать на ревью

Работающее, но плохо структурированное решение будет считаться невыполненным.
